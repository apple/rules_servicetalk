# Zip Protos

Zip Protos is a simple addition to the protobuf rules to allow creating a `.jar` file from the protobuf so they can be 
included in an exported java library. See extract proto for the reason this is useful.

This directory is both an example of how to use the `zip_proto` macros and a test to verify it still works correctly.